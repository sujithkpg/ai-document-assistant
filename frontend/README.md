# RAG Document Assistant UI

Angular frontend for AI Document Assistant.

## Run

```bash
cd frontend
npm install
npm start
```

Frontend runs on:

```text
http://localhost:4200
```

The proxy forwards `/api` requests to Spring Boot at `http://localhost:8080`.
